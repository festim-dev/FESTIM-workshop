name: festim-workshop
channels:
  - conda-forge
  - defaults
dependencies:
  - fenics
  - matplotlib==3.7.1
  - meshio[all]
  - numpy==1.24
  - pip>=20.1
  - pip:
    - git+https://github.com/festim-dev/festim@main  # TODO change this once 1.3 is released
    - pyparsing
    # - ipykernel
    - h-transport-materials==0.12.7
    - nbconvert